<UserControl x:Class="ReNamer.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="24"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtValue"
                         Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, TargetNullValue=0}"
                         MouseWheel="txtValue_MouseWheel"
                         VerticalContentAlignment="Center"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Button Grid.Row="0"  Click="Increase_Click">
                <Path
                    Data="M 0 6 L 5 0 L 10 6 Z"
                    Stretch="Uniform"
                    Width="8"
                    Height="6"
                    Fill="Black"/>
            </Button>
            <Button Grid.Row="1" Click="Decrease_Click" >
                <Path
                    Data="M 0 0 L 5 6 L 10 0 Z"
                    Stretch="Uniform"
                    Width="8"
                    Height="6"
                    Fill="Black"/>
            </Button>
        </Grid>

    </Grid>
</UserControl>

<hc:Window
    x:Class="ReNamer.Views.FileNameInputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:ReNamer.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ReNamer.ViewModels"
    Title="{Binding Title}"
    Width="300"
    Height="180"
    d:DataContext="{d:DesignInstance Type=vm:FileNameInputDialogViewModel,
                                     IsDesignTimeCreatable=False}"
    mc:Ignorable="d">
    <StackPanel Margin="20">
        <TextBlock Margin="0,0,0,10" Text="{Binding Tips}" />
        <TextBox hc:InfoElement.Placeholder="不能包含 \ / : * ? &lt; &gt; |" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
        <StackPanel
            Margin="0,20,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="60"
                Margin="0,0,10,0"
                Command="{Binding ConfirmCommand}"
                Content="确定"
                IsDefault="True" />
            <Button
                Width="60"
                Command="{Binding CancelCommand}"
                Content="取消"
                IsCancel="True" />
        </StackPanel>
    </StackPanel>
</hc:Window>
